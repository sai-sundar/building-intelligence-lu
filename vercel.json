{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd frontend && npm install && npm run build",
  "outputDirectory": "frontend/dist",
  "framework": null,
  "functions": {
    "api/**/*.js": { "includeFiles": "frontend/src/data/**" }
  },
  "rewrites": [{ "source": "/api/(.*)", "destination": "/api/$1" }]
}
