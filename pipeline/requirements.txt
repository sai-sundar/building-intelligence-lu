# Data pipeline dependencies.
# Note: no geopandas/shapely — the pipeline joins tabular census data onto a
# pre-projected commune GeoJSON by commune name, so no spatial operations are
# needed. This keeps the pipeline installable without GDAL.
requests==2.32.5
pandas==2.3.3
